\n\"\"\"\nThis script demonstrates that multiprocessing provides a significant performance\nbenefit for CPU-bound tasks by bypassing the GIL.\n\n`multiprocessing` creates separate processes, each with its own Python interpreter\nand memory space. This allows CPU-bound tasks to run in true parallel on\nmultiple CPU cores, bypassing the limitation imposed by the GIL.\n\nExpected Outcome:\nRunning this will show that the multiprocessing execution time is roughly half\nof the sequential execution time (from the other script), as the tasks run in\nparallel on different cores.\n\"\"\"\nimport time\nfrom multiprocessing import Process\n\n# A constant for the number of iterations in our CPU-bound task\n# Note: This might need to be adjusted based on your machine's performance.\nCOUNT = 50_000_000\n\ndef cpu_bound_task():\n    \"\"\"A task that performs a heavy calculation to keep the CPU busy.\"\"\"\n    print(\"Starting CPU-bound task...\")\n    start_time = time.perf_counter()\n    for i in range(COUNT):\n        i * i # A simple calculation\n    end_time = time.perf_counter()\n    print(f\"Task finished after {end_time - start_time:.2f} seconds.\")\n\ndef run_with_multiprocessing():\n    \"\"\"Runs the CPU-bound task twice using separate processes.\"\"\"\n    start_time = time.perf_counter()\n\n    # Create two process objects\n    process1 = Process(target=cpu_bound_task)\n    process2 = Process(target=cpu_bound_task)\n\n    # Start the processes\n    process1.start()\n    process2.start()\n\n    # Wait for both processes to complete\n    process1.join()\n    process2.join()\n\n    end_time = time.perf_counter()\n    print(f\"Total time: {end_time - start_time:.2f} seconds.\")\n\nif __name__ == \"__main__\":\n    # On some platforms (like Windows), you must protect the main-routine\n    # code with a `if __name__ == '__main__':` block.\n    print(\"--- Multiprocessing Execution ---\")\n    run_with_multiprocessing()\n