\n\"\"\"\nThis script demonstrates that threading provides NO performance benefit for\nCPU-bound tasks due to the Global Interpreter Lock (GIL).\n\nA CPU-bound task is one that spends most of its time doing computations.\nWe simulate this with a simple but intensive calculation loop.\n\nExpected Outcome:\nRunning this script will show that the threaded execution time is roughly the\nsame as (or even slightly slower than) the sequential execution time. The GIL\nprevents the threads from running in parallel on multiple CPU cores.\n\"\"\"\n\nimport time\nimport threading\n\n# A constant for the number of iterations in our CPU-bound task\COUNT = 50_000_000\n\ndef cpu_bound_task():\n    \"\"\"A task that performs a heavy calculation to keep the CPU busy.\"\"\"\n    print(\"Starting CPU-bound task...\")\n    start_time = time.perf_counter()\n    for i in range(COUNT):\n        i * i # A simple calculation\n    end_time = time.perf_counter()\n    print(f\"Task finished after {end_time - start_time:.2f} seconds.\")\n\ndef run_sequentially():\n    \"\"\"Runs the CPU-bound task twice in a row.\"\"\"\n    start_time = time.perf_counter()\n    cpu_bound_task()\n    cpu_bound_task()\n    end_time = time.perf_counter()\n    print(f\"Total time: {end_time - start_time:.2f} seconds.\")\n\ndef run_with_threads():\n    \"\"\"Runs the CPU-bound task twice using threads.\"\"\"\n    start_time = time.perf_counter()\n\n    # Create two thread objects\n    thread1 = threading.Thread(target=cpu_bound_task)\n    thread2 = threading.Thread(target=cpu_bound_task)\n\n    # Start the threads\n    thread1.start()\n    thread2.start()\n\n    # Wait for both threads to complete\n    thread1.join()\n    thread2.join()\n\n    end_time = time.perf_counter()\n    print(f\"Total time: {end_time - start_time:.2f} seconds.\")\n\nif __name__ == \"__main__\":\n    print(\"--- Sequential Execution ---\")\n    run_sequentially()\n    print(\"\\n--- Threaded Execution ---\")\n    run_with_threads()\n