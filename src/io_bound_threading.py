\n\"\"\"\nThis script demonstrates that threading provides a significant performance\nbenefit for I/O-bound tasks.\n\nAn I/O-bound task is one that spends most of its time waiting for external\nresources, like a network response or a disk read. We simulate this with\n`time.sleep()`.\n\nExpected Outcome:\nRunning this script will show that the threaded execution time is roughly half\nof the sequential execution time, because the GIL is released during `time.sleep()`,\nallowing the other thread to run concurrently.\n\"\"\"\n\nimport time\nimport threading\n\n# A simulated I/O-bound task\def io_bound_task():\n    \"\"\"A task that simulates waiting for I/O by sleeping for 1 second.\"\"\"\n    print(\"Starting task...\")\n    time.sleep(1)\n    end_time = time.time()\n    print(f\"Task finished after {1:.2f} seconds.\")\n\ndef run_sequentially():\n    \"\"\"Runs the I/O-bound task twice in a row.\"\"\"\n    start_time = time.perf_counter()\n    io_bound_task()\n    io_bound_task()\n    end_time = time.perf_counter()\n    print(f\"Total time: {end_time - start_time:.2f} seconds.\")\n\ndef run_with_threads():\n    \"\"\"Runs the I/O-bound task twice using threads.\"\"\"\n    start_time = time.perf_counter()\n\n    # Create two thread objects\n    thread1 = threading.Thread(target=io_bound_task)\n    thread2 = threading.Thread(target=io_bound_task)\n\n    # Start the threads\n    thread1.start()\n    thread2.start()\n\n    # Wait for both threads to complete\n    thread1.join()\n    thread2.join()\n\n    end_time = time.perf_counter()\n    print(f\"Total time: {end_time - start_time:.2f} seconds.\")\n\nif __name__ == \"__main__\":\n    print(\"--- Sequential Execution ---\")\n    run_sequentially()\n    print(\"\\n--- Threaded Execution ---\")\n    run_with_threads()\n